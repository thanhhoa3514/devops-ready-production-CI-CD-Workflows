# Docker Hub account name that owns the image.
DOCKERHUB_USERNAME=your-dockerhub-username

# Image tag to deploy to production. Use a validated sha tag or a release tag like v1.1.2.
IMAGE_TAG=sha-<validated_commit_sha>

# Host port exposed by docker compose.
APP_PORT=3000
