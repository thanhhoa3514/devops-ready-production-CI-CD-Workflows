# Docker Hub account name that owns the image.
DOCKERHUB_USERNAME=your-dockerhub-username

# Image tag to deploy. For manual deploys use latest or a specific sha tag.
IMAGE_TAG=latest

# Host port exposed by docker compose.
APP_PORT=3000
