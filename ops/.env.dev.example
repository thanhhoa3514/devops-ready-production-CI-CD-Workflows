# Docker Hub account name that owns the image.
DOCKERHUB_USERNAME=your-dockerhub-username

# Image tag to deploy to dev. The dev workflow updates this automatically.
IMAGE_TAG=latest

# Host port exposed by docker compose. Keep it different from staging and production on a shared VPS.
APP_PORT=3002
